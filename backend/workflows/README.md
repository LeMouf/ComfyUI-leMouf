# Backend Workflows Module

This package contains workflow-oriented backend helpers:

- `catalog.py`
  - feature-scoped workflow discovery and safe file loading
- `profiles.py`
  - workflow profile extraction/normalization/routing helpers

Used by `nodes.py` through compatibility wrappers.
